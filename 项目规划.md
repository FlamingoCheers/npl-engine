# NPL 项目规划 — 叙事编程系统 v0.1

> 本文档由《初始设计思路.md》（与 GPT 的设计对话）整理而成，是 NPL 项目的可执行开发规划。
> 设计对话负责"为什么"和"是什么"，本文档负责"怎么做"。
> 已确认的三项关键决策：
> 1. **LLM 接入**：适配器层，同时支持线上 OpenAI 兼容 API（DeepSeek/Qwen/智谱等）与本地模型（Ollama 等）。
> 2. **语言形态**：v0.1 直接实现 `.npl` 文本语法 + 解析器（不做 Python 内嵌 DSL 过渡）。
> 3. **M1 范围**：火车站垂直切片（两人车站相遇，A 知道 B 背叛了自己，B 不知道 A 已经知道）。

---

## 1. 项目定位与设计原则

### 1.1 一句话定位

NPL（Narrative Programming Language）是一个**叙事编程系统**：用机器可处理的 DSL 描述故事世界、人物认知、事件因果、叙事视角与文学表现，由一个类似编译器的系统把它"编译"成自然语言小说。

```
Narrative Program → Compiler → Scene Runtime → LLM → Prose Renderer → 小说
```

它与"给 AI 一个很长的 prompt"是完全不同的范式：**小说首先被表示成一个可执行的叙事程序，文本只是这个程序的一次编译产物。**

### 1.2 核心设计原则（全项目不可违背）

1. **Narrative Runtime is the source of truth; prose is only a projection.**
   叙事运行时才是真实，小说文本只是它的一次投影。所有校验、检查、回溯都针对 Runtime 状态，而不是针对文本。
2. **让机器负责"不可违背的东西"，让模型负责"无法穷举的东西"。**
   确定性的（世界真相、因果、状态、知识、权限、时间、视角、信息释放、结构、风格约束）交给程序；概率性的（语言、意象、节奏、隐喻、对话、微妙心理、场景质感）交给 LLM。
3. **语义上精确，概念上文学化。**
   DSL 本身不写成文学腔（禁止 `the rain remembered...` 式语法），但语言原语直接取自文学理论：`foreshadow / withhold / misdirect / echo / reveal / contrast / motif / anticipate / recontextualize / silence`。
4. **不要把所有东西写进 DSL。**
   一旦语言退化成 `sentence_1 = ...; emotion = 0.73` 的模板系统，项目就失败了。LLM 的价值恰恰在那些无法被完全形式化的文学生成上。

### 1.3 v0.1 明确的非目标（防止范围蔓延）

- 互动叙事 / AI DM / 游戏分支（远期汇合方向，v0.1 不做）
- 角色自由 Agent 演进（v0.1 角色是脚本驱动的确定性事件流；LLM Actor 模式放 M3）
- 多 POV 自由切换渲染、`--compact / --cinematic` 等多形态输出（M4 后实验）
- 语料库风格提取管线（M4；规划中已预留接口）
- GUI / 编辑器

---

## 2. 计算模型（从设计思路固化的规范）

### 2.1 两个核心公式

```
N(t+1) = T(N(t), E(t), A(t), R(t))
```
- `N` = Narrative State（叙事状态）　`T` = Transition　`E` = Event　`A` = Agency（角色行动）　`R` = Revelation（信息揭示）

```
Text(t) = Render(N(t), Scene(t), POV(t), Style)
```
**文本不是世界本身，而是叙事状态的一种渲染结果。**

### 2.2 六层叙事状态（Narrative State Vector）

| 层 | 回答的问题 | v0.1 实现程度 |
|---|---|---|
| World State | 世界（上帝视角）实际上是什么？对象、地点、关系、属性、真相 | 完整 |
| Temporal State | world_time / narrative_order / scene_time 三者分离（发生顺序 ≠ 讲述顺序） | 简化：线性时间 + narrative_order 字段预留 |
| Character State | 人物动态状态：Body/Emotion/Goals/Intentions/Memory/Beliefs/Knowledge/Relationships/Personality/Dispositions。Personality 是慢变量，Emotion/Intention/Belief 是快变量 | 完整（慢/快变量二分） |
| Epistemic State | 谁知道什么、相信什么、误解什么（Knowledge Graph of Minds，含嵌套认知） | 完整（v0.1 嵌套一层，M3 多层） |
| Narrative State | 作者正在怎么讲：reader_knows、dramatic questions、active mysteries、withheld、planned revelation | 完整 |
| Presentation State | 当前镜头允许呈现什么：POV access 的 allow/deny 清单 | 完整（权限式渲染） |

### 2.3 三个世界（NPL 的核心计算概念）

```
Reality（实际发生了什么）
Character Mind（每个人以为发生了什么）
Reader Model（读者以为发生了什么）
```

三者可以完全不一致，**文学叙事最迷人的部分恰恰发生在它们不一致的时候**。所有可计算文学结构都建立在这三者差异之上（见 §2.7）。

### 2.4 Information Object（信息是一等公民）

```text
Information {
    id            # 如 "killer_identity"
    truth         # 对应的世界真相
    known_by      # 持有者
    suspected_by  # 怀疑者（附 confidence）
    unknown_to    # 明确不知情者
    public        # 是否公开
}
```

小说是一台**信息流动机器**：信息被 产生 → 发现 → 理解 → 误解 → 隐藏 → 传播 → 伪装 → 揭示。戏剧性 = 信息在主体间流动产生的差异。

### 2.5 Scene 与 Event 的定义

- **Scene**：在连续时空中、由一个特定叙事配置（POV + access）观察的一段状态转换。不是"一段文字"。字段：Time / Location / Participants / POV / Initial State / Events / Character Actions / Information Changes / Emotional Changes / Dramatic Intent / Ending State。
- **Event**：对 Narrative State 产生**可验证改变**的 Transition（改 World 事实、改人物 knowledge、推进 mystery_progress），可触发因果链（Causal Graph）。"发生了一件事"但什么状态都没变的，不是 Event。

### 2.6 角色决策环（两种模式）

```
observes() → evaluates() → decides() → acts()
Action = f(perception, knowledge, beliefs, goals, emotion, personality, situation)
```

- **v0.1 脚本模式（默认）**：角色的行动由 .npl 里的 events 脚本确定性驱动，保证可验证、可回归测试。
- **M3 Actor 模式（可选）**：角色决策由 LLM 在其 epistemic sandbox 内生成，Narrative Intent 做软约束兜底。

### 2.7 可计算的文学结构（检查器与原语的理论依据）

| 文学现象 | 计算结构 |
|---|---|
| 悬疑 | Information Asymmetry（信息不对称） |
| 戏剧性误会 | Character Belief ≠ World Truth |
| 反转 | Reader Belief → Recontextualization |
| 伏笔 | Early Event → Later Meaning |
| 限制视角 | Presentation Access ⊆ Character Epistemic State |
| 不可靠叙述者 | Narrator Belief ≠ World Truth |
| 意识流 | Presentation follows Mental State |

### 2.8 四级约束（冲突时上层优先）

1. **World Constraint**（绝对）：死人不走路。
2. **Epistemic Constraint**（认知）：角色不能凭空知道；POV 不能看到权限外的信息。
3. **Narrative Constraint**（叙事设计）：`murderer_identity` 在第 20 章前必须保持隐藏。
4. **Stylistic Constraint**（风格倾向）：情绪应当展示而非陈述。

### 2.9 LLM 的角色与 Context Compiler

LLM **不是 Runtime**，是 Probabilistic Renderer / Actor。LLM 每次只获得：

```
Context = Query(NarrativeState, Scene, Agent, POV, Task)
        = 当前场景 + 相关世界状态 + POV 角色状态 + 相关记忆/知识 + 叙事意图 + 风格规则
```

而**永远不喂**整本小说、全部人物、全部历史。这就是系统解决 Context Window 问题的根本手段。

---

## 3. NPL 语言 v0.1

### 3.1 词汇表分层

- **核心 10 原语（v0.1 实现）**：`WORLD ENTITY STATE EVENT CHARACTER KNOWLEDGE BELIEF INTENTION SCENE RENDER`
- **派生层（M2-M3 逐步引入）**：`SECRET MEMORY RELATIONSHIP POV REVELATION FORESHADOW MISDIRECTION MOTIF EMOTION STYLE`
- **结构层（M3-M4）**：`CHAPTER ARC ACT PLOT NOVEL`

语法 v0.1 刻意最小化，只覆盖核心 10 原语 + `information`（Information Object）+ `access`（POV 权限块）+ `dramatic_goal`。语言版本号写入源文件头（`npl@0.1`），为演进留 migration 空间。

### 3.2 语法（v0.1 已定稿；权威版本见 docs/语言规范.md，M0 已实现）

```ebnf
program           := VERSION NEWLINE statement+ EOF      ; VERSION 即 "npl@0.1"
statement         := world_decl | character_decl | information_decl
                   | scene_decl | render_decl

world_decl        := "world" IDENT "{" world_item* "}"
world_item        := "location" "=" IDENT
                    | "time" "=" TIMESTAMP
                    | "fact" IDENT "=" BOOLEAN
                    | "entity" IDENT "{" entity_attr* "}"
entity_attr       := IDENT "=" (IDENT | NUMBER | BOOLEAN | STRING)

character_decl    := "character" IDENT "{" char_section* "}"
char_section      := ("knows"|"believes"|"suspects"|"does_not_know"|"intends"|"goal")
                     ":" item_list                       ; 同行 a, b 或逐行
                   | ("personality"|"emotion") ":" trait_list
trait             := IDENT "=" NUMBER                    ; 强度 0~1

information_decl  := "information" IDENT "{" info_field* "}"
info_field        := "truth" "=" IDENT                   ; 必填，须为已声明 fact
                    | "known_by" "=" id_list
                    | "unknown_to" "=" id_list
                    | "suspected_by" "=" "[" [IDENT "=" NUMBER ("," ...)*] "]"
                    | "public" "=" BOOLEAN

scene_decl        := "scene" STRING "{" scene_item* "}"
scene_item        := "pov" "=" IDENT                     ; 必填
                    | "location" "=" IDENT
                    | "world_time" "=" TIMESTAMP
                    | "participants" "=" id_list         ; 必填
                    | "access" "{" access_rule* "}"
                    | "events" "{" event_ref* "}"
                    | "information_changes" "{" event_ref* "}"
                    | "dramatic_goal" "{" goal_item* "}"
                    | "emotional_arc" "{" arc_entry* "}"
access_rule       := ("allow"|"deny") "=" IDENT "." IDENT ; 主体.能力
event_ref         := IDENT "." IDENT ["(" [IDENT ("," IDENT)*] ")"]
goal_item         := ("reveal"|"conceal") "=" IDENT
arc_entry         := IDENT ":" IDENT ("->" IDENT)*

render_decl       := "render" "{" render_item* "}"
render_item       := ("style"|"language") "=" IDENT
```

人物能力限定：`perception / memory / inference / private_thought / emotion / intention`；`world` 仅有 `truth`（`allow = world.truth` 即声明全知视角）。无 `access` 块时缺省策略：允许 pov 的 perception/memory/inference，拒绝他人 private_thought 与 world.truth。

### 3.3 完整示例：火车站垂直切片（examples/station/station.npl）

此文件同时是 **M1 的验收样例**：

```narrative
npl@0.1

world station_world {
    location = old_station
    time = 2047-03-17 23:40

    fact bao_signed_the_transfer = true      // 世界真相：B 已把厂子卖了
    fact bao_met_buyer_march_3rd = true
    fact lin_has_seen_contract_copy = true   // A 拿到过合同复印件
}

information betrayal_secret {
    truth = bao_signed_the_transfer          // 秘密本体：双方都知道
    known_by = [Lin, Bao]
    public = false
}

information meta_knowledge {                 // 元认知不对称："B 不知道 A 已经知道"
    truth = lin_has_seen_contract_copy
    known_by = [Lin]
    unknown_to = [Bao]                       // B 不知道 A 已经知道
    public = false
}

character Lin {                              // A：知道对方背叛，不动声色
    knows:
        bao_signed_the_transfer
        bao_met_buyer_march_3rd
        lin_has_seen_contract_copy
    believes:
        marriage_is_effectively_over         // 自由命题：信念，无对应世界事实
    intends:
        confirm_before_confronting
    personality:
        restrained = 0.9
        observant = 0.8
    emotion:
        grief = 0.7
        control = 0.6
}

character Bao {                              // B：背叛者，以为瞒得很好
    knows:
        bao_signed_the_transfer
    believes:
        lin_knows_nothing                    // 错误信念（≠ 世界真相）
    intends:
        delay_telling_until_after_festival
    personality:
        evasive = 0.7
    emotion:
        guilt = 0.8
        anxiety = 0.5
}

scene "夜车" {
    location = old_station
    world_time = 2047-03-17 23:40

    pov = Lin

    participants = [Lin, Bao]

    access {
        allow = Lin.perception
        allow = Lin.memory
        allow = Lin.inference
        deny  = Bao.private_thought          // 渲染器无权进入 B 的内心
        deny  = world.truth                  // 无权做全知旁白
    }

    events {
        Bao.arrives_late
        Bao.mentions_buyer_casually          // 说漏一个只有买家才知道的细节
        Lin.notices_detail_mismatch
        Lin.asks_ordinary_questions          // 平静地问日常问题（试探）
    }

    information_changes {
        Lin.confirms(bao_met_buyer_march_3rd)  // 证据链闭合，但不动声色
    }

    dramatic_goal {
        reveal  = meta_knowledge             // 对读者揭示"她已经知道"（微表情/沉默）
        conceal = lin_has_seen_contract_copy // 对 Bao、也暂时对读者隐藏实证
    }

    emotional_arc {
        Lin:  grief -> control -> cold_clarity
        Bao:  anxiety -> relief -> unease
    }
}

render {
    style = restrained_literary
    language = zh
}
```

### 3.4 语法演进策略

- M0 只锁核心 10 原语的最小语法；每个里程碑允许向后不兼容变更，但必须 bump 版本号并提供迁移说明。
- 派生原语（`secret/motif/foreshadow...`）等 Runtime 语义验证可行后再进入语法，避免"漂亮但没用"的关键字。

---

## 4. 系统架构

### 4.1 编译管线

```
.npl 源码
   │  Lexer / Parser（手写递归下降）
   ▼
AST
   │  Semantic Validator（引用检查、类型检查、约束声明检查）
   ▼
Runtime 初始化（六层状态）
   │  Scene Executor（确定性事件流 → 状态转换 + 快照）
   ▼
Scene IR（POV / Visible / Inferable / Forbidden / Emotional trajectory / Narrative objectives）
   │  Context Compiler（按 Task 动态组装最小上下文）
   ▼
LLM Renderer（适配器层，仅获得允许信息）
   ▼
散文文本
   │  Post-Checker（命题抽取 + 认知泄漏规则检查）
   ▼
通过 → 落盘 ｜ 未通过 → 带 NAR 错误码重渲染（上限 N 次）
```

### 4.2 模块职责

| 模块 | 职责 |
|---|---|
| `lexer / parser / ast` | .npl → AST，带行号的友好报错 |
| `validator` | 语义校验（未声明引用、participants 缺失、access 引用非法等） |
| `runtime/state` | 六层状态的内存模型 + JSON 快照/恢复/diff |
| `runtime/executor` | 场景执行：应用 events → 更新 World/Character/Epistemic/Narrative 状态，产出 Scene IR 与 Ending State |
| `context/compiler` | 按 (NarrativeState, Scene, Agent, POV, Task) 组装最小 LLM 上下文，**物理上不包含 deny 信息** |
| `render/adapter` | LLM 适配器层（见 §4.3） |
| `render/renderer` | 渲染 prompt 模板、重试循环、风格约束注入 |
| `check/extract` | LLM 命题抽取：把生成的散文转成结构化命题（谁知道了什么） |
| `check/epistemic` | 规则引擎：命题 × Epistemic State → NAR 错误码 |
| `style` | M4：style 块 → 渲染约束；预留语料特征提取接口 |

### 4.3 LLM 适配器层（已确认决策）

统一走 **OpenAI 兼容协议**（一套 SDK 覆盖绝大多数供应商），另留本地直连通道：

```jsonc
// npl.config.json
{
  "adapters": {
    "deepseek": { "type": "openai-compatible", "base_url": "https://api.deepseek.com/v1", "model": "..." },
    "qwen":     { "type": "openai-compatible", "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1", "model": "..." },
    "local":    { "type": "openai-compatible", "base_url": "http://localhost:11434/v1", "model": "qwen2.5:14b" }
  },
  "render":  { "adapter": "deepseek" },
  "extract": { "adapter": "local" }   // 命题抽取可跑本地小模型，省钱
}
```

- 不同任务可绑不同适配器（渲染用大模型、抽取用本地模型）。
- API key 走环境变量，禁止入库。
- 适配器接口需支持：`chat(messages, temperature, json_mode?)`，抽取任务要求 JSON 输出。

### 4.4 状态存储与快照

- `build/state_snapshots/scene_XXX.json`：每个场景执行后的六层状态快照。
- 用途：`inspect` 查询任意时点认知；diff 相邻快照看"这一幕改变了什么"；确定性回归测试（同样输入 → 同样状态演进）。
- `build/scene_ir/scene_XXX.json`：Scene IR。
- `build/prose/scene_XXX.md` + `build/reports/scene_XXX.json`（检查报告）。

---

## 5. 代码仓库结构

```
48-npl/
├── 初始设计思路.md            # 原始设计对话（只读，理念来源）
├── 项目规划.md                # 本文档
├── docs/
│   ├── 语言规范.md            # M0 定稿：EBNF + 语义规则 + 错误码
│   └── 计算模型.md            # M1 沉淀：六状态的字段级定义
├── examples/
│   └── station/               # 火车站垂直切片（M1 验收样例）
│       ├── station.npl
│       └── expected/          # 人工确认的 inspect/check 基准输出
├── src/npl/
│   ├── __init__.py
│   ├── cli.py                 # npl 命令入口（argparse）
│   ├── lexer.py
│   ├── parser.py
│   ├── ast_nodes.py
│   ├── validator.py
│   ├── runtime/
│   │   ├── state.py
│   │   ├── executor.py
│   │   └── snapshot.py
│   ├── ir/
│   │   └── scene_ir.py
│   ├── context/
│   │   └── compiler.py
│   ├── render/
│   │   ├── adapter.py
│   │   ├── renderer.py
│   │   └── prompts.py
│   ├── check/
│   │   ├── extract.py
│   │   └── epistemic.py
│   └── style/
│       └── rules.py           # M4 前只含手工 style 预设
├── tests/                     # 单测 + 确定性回归
├── build/                     # 编译产物（gitignore）
├── npl.config.example.json
└── pyproject.toml
```

技术栈：**Python 3.11+**；解析器**手写递归下降**（语法小、报错可控、零依赖）；LLM 调用用 `openai` SDK（兼容协议）；其余尽量 stdlib。

---

## 6. CLI 命令集

```bash
npl validate examples/station/station.npl          # 语法 + 语义校验，报错误码与行号
npl simulate examples/station/station.npl          # 只跑 Runtime：产出快照 + Scene IR，不调 LLM
npl render  examples/station/station.npl --scene 1 # 渲染指定场景（含渲染后检查与重试）
npl check   examples/station --scene 1             # 对已生成文本跑认知泄漏检查
npl inspect examples/station --character Lin --scene 1
                                                   # 查询 Lin 在场景 1 结束时的认知快照
npl diff    examples/station --scene 1             # 相邻状态快照 diff（这一幕改变了什么）
```

`inspect` 目标输出形态（对齐设计对话）：

```
Lin @ scene "夜车" 结束时:
  ✓ knows: bao_signed_the_transfer, bao_met_buyer_march_3rd
  ✓ confirmed: bao_met_buyer_march_3rd
  ✗ does_not_know: (无)
  believes: marriage_is_effectively_over
  虚假信念: 无
Bao @ scene "夜车" 结束时:
  believes: lin_knows_nothing        ← 虚假信念（世界真相：lin_has_seen_contract_copy）
```

---

## 7. 里程碑与验收标准

> 估时按单人业余开发节奏，可整体压缩或拉伸。

### M0 — 语言与编译器骨架（约 1-2 周）✅ 已完成（35 项测试；12 个错误样例全命中）

- 交付：EBNF 定稿（docs/语言规范.md）；lexer/parser/validator；`npl validate`。
- 内容：核心 10 原语 + information + access + dramatic_goal 的最小语法；错误信息带行号与错误码（NAR-001x）。
- **验收**：`examples/station/station.npl` 通过校验；对 10 个故意错误样例（缺 pov、引用未声明人物、access 引用非法等）全部给出正确错误码与行号。

### M1 — 火车站垂直切片（约 2-3 周）★ 第一个端到端里程碑 ✅ 已完成（真实 LLM：智谱 glm-4-flash 免费通道）

- 交付：Runtime（六层状态，Temporal 简化）→ Scene Executor → Scene IR → Context Compiler → Renderer（适配器层）→ 中文散文；`npl simulate / render / inspect`。
- 关键机制：**权限式渲染**——Context Compiler 组装的上下文里物理上不存在 `deny` 的信息（Bao 的内心、世界全知真相）。
- **验收**：
  1. 全链路跑通，`render --scene 1` 产出 800-1500 字中文散文（mock 与真实 LLM 均已跑通）；✅
  2. inspect 输出与 §6 目标形态一致（含 Bao 的虚假信念标注）；✅
  3. 附录 B 验收清单：工程项已通过；散文质量项待真实 LLM 渲染后人工评审；
  4. simulate 是纯确定性的：同输入 → 快照逐字节一致（回归测试锁定）。✅（54 项测试全通过）

### M2 — 静态检查器（约 1-2 周）✅ 已完成

- 交付：命题抽取（LLM，双次并集）+ 认知泄漏规则引擎；`npl check / diff`；错误码体系（附录 A）；行尾注释语义链（AST desc → Context Compiler 权限过滤 → prompt 语义翻译，见 §3.2 注）。
- **验收结果**：
  1. 3 个故意泄漏样例全部以正确错误码命中（全知旁白/角色全知/conceal 违规）；
  2. **对照实验已跑完**（experiments/report_2026-08-28.md，渲染+判定均为 deepseek-v4-flash，n=10）：A 组（裸 LLM + 全量背景）泄漏率 **9/9**，B 组（NPL 权限管线）**1/10**（唯一命中为目击闪回对读者的实证暗示，NAR-031 真语义命中）。
- 实验沉淀的两条规则语义：①021a 戏剧反讽豁免——叙述断言信念锚定事实的世界真值（"她并非一无所知"）是 reveal meta_knowledge 的合法机制，由 conceal 规则另行管辖；②031 双路严格语义——conceal 一旦在文中被断言（含暗示级 evidence span）即告破。

### M3 — 认知引擎与多场景（约 3-4 周）✅ 已完成

- 交付：Information Object 流转语义（CONFIRM/REVEAL/MISUNDERSTAND/SUSPECT/HIDE 五动词族）；嵌套认知一层（`A believes B.does_not_know(x)`，believes_about + inspect 三态标注）；可选 LLM Actor 模式（`npl actor` epistemic sandbox + JSON 行动提案）；多场景 + 闪回（flashback 标记，状态按叙事顺序演化，渲染禁后见之明）；Reader Model（reader_knows / suspicions / unanswered_questions，`inspect --reader`）。
- **验收**（examples/novel/chapter_one.npl《定金》5 场景，倒叙开场 + 信息逐步释放）：
  1. `validate / simulate` 全通；两次模拟快照 MD5 逐字节一致（确定性）；
  2. `inspect` 任意角色任意时点：Bao@终幕同时呈现虚假信念（lin_knows_nothing）、嵌套信念标注（believes_about[Xiao] 与事实一致）、本幕新确认标记；Xiao@第3幕严格限制视角；
  3. conceal 整章守住：`npl check` 抓出渲染产物中全部实证/暗示级违规（复印件意象、烧纸数字红章、他看见那张纸），修复后终幕 0 违规；检查器同时抓出源文件两处认知授权缺失（Bao 作为交易当事人缺 buyer_paid_deposit / bao_met_buyer_march_3rd）——"编译器当裁判"再次自证；
  4. 抽取缓存（build/extract_cache，按 prompt 版本+输入哈希）使迭代判定秒级复跑。
- 注：《定金》全章最终验收达成——离线模式（`dump_ctx.py` 导出权限过滤上下文 → GLM 渲染/抽取 → `rejudge_chapter.py` 离线判定）：**全章 0 ERROR / 0 WARNING**，五幕 800–1500 字达标，conceal 整章守住（第 4 幕抵押 reveal 达成后，终幕正片只呈现"她知道但不说"）。

### M4 — 风格系统与文学原语 ✅（2026-08-29）

- 交付：`style` 块 → 渲染约束（desc / sentence_max / emotion_naming / sensory / dialogue_gaps / rule，预设打底+声明覆写，`resolve_style`）；文学原语 `motifs`（introduce/recurrence/final 三幕结构）/ `foreshadows` / `withholds`（`until = N` 运行时展开为临时 conceal）/ `misdirects`（misdirects_active + Reader Model suspicions）全部入语法、运行时与检查器；语料风格指纹接口预留（`style/rules.py: corpus_fingerprint`）。
- 新错误码：NAR-052（长句占比>30% 警告）、NAR-061/062/063（母题结构静态校验）、NAR-064（声明母题未呈现）、NAR-065（伏笔无回收）、NAR-066（扣留未按期释放）、NAR-067（误导目标为真事实/无反转载体）。
- 章节演示（《定金》）：母题「茶杯」S1 引入 → S2 复现（凉透的茶）→ S5 收束（一口没动）；S1 伏笔"定金电话"→ S2 回收（buyer_paid_deposit reveal）；S1 withhold `lin_has_seen_contract_copy until = 4` → S4 书房 reveal 释放；S3 误导 `bao_burning_letters`（"写坏了的信，大概是"）→ S4 抵押 reveal 反转。
- **验收达成**：106 测试全过；simulate 确定性（快照 MD5 逐字节一致）；全章 check（含结构检查与 052 句长）**0 ERROR / 0 WARNING**；盲评**已通过**（2026-08-29，用户正确识别 restrained_literary 版本，双风格差异清晰可辨，材料在 `experiments/blind_eval/`）。**M0–M4 里程碑计划全部完成。**

### v0.2 — 语言扩展与语料风格指纹 ✅（2026-08-29）

用户指令："先做v0.2 语言扩展外加语料风格指纹。这个指纹需要是可扩展的，这套系统拿出去，谁都能写出不同文风的东西。"

- **语言扩展**：①fact 值扩展为布尔/数值/枚举（NAR-044）；②嵌套认知任意深度（递归 EBNF，canonical 化存储 `nested_beliefs`，inspect/prompt 展示与接地标注；运行时转换验证深度仍为 1，明示于文档）；③`import` 多文件程序（单次包含/环检测 NAR-016/缺失 NAR-015/合并冲突 NAR-011；场景顺序=导入在前入口在后，叙事时序由文件组织表达；母题三幕链跨文件校验）；④版本白名单 `npl@0.1 | npl@0.2`。
- **文学基础设施**：`npl motifs` 意象聚类（jieba 分词 + 停用词 + 跨幕字符 bigram 重聚，声明母题 ★标注）；检查器 NAR-064 中文母题字面回退（无 LLM 也可跑）。
- **语料风格指纹（核心交付）**：`src/npl/style/features.py` 特征注册表（`@register_feature` 开放注册 + `LEXICONS` 词表整体可注入/新类别透传）+ 内置 8 特征（句长/段落中位/对话比/情绪命名率/比喻密度/感官密度/词汇丰富度/标点谱，均千词归一）；`fingerprint.py` 确定性指纹（fingerprint_version + feature_set_version(sha1) + lexicon_digest 入 meta，可 diff 可入库）+ `to_style_decl` 确定性映射 + `render_style_npl` 直出 style 块；CLI `npl style fingerprint / show / to-npl / diff`。
- **扩展性验证**：测试内用 `@register_feature("color_word_rate")` + 新词表类别 `color_words` 走通第三方扩展路径；词表注入改变指纹与 digest。
- **验收达成**：144 测试全过；盲评 A/B 语料指纹 diff 与盲评结论一致（句长 +11.1、比喻密度 +10.6/千词、情绪命名 0→2.9/千词、段落中位 55→127）；指纹生成的 style 块可被 parser 直接解析（round-trip 测试）。文档三件套已同步（语言规范 v0.2 / 计算模型 v0.5 / 本文件）。

### v0.3 — 内核深化（已排期，2026-08-29 用户批准）

范围（用户确认：规划内剩余只做前两项 + 快照续跑；互动叙事前端为远期）：

1. **跨章长程母题追踪**：母题目前按单文件/单章声明（三幕链合并后校验），跨章节的长程追踪（出现频率曲线、间隔衰减、跨文件统计检验）未做。目标：`npl motifs` 升级为可跨 build 目录/多文件的追踪报告，为长篇连载服务。**✅ 2026-08-29 完成**——`analysis/tracking.py` 轨迹合并（词族/bigram 相交）+ 全文回扫计数（单次出现章不计为断线）+ 状态机（活跃/收尾/间歇/断线 + 千字频率趋势）+ CLI 跨章模式（`npl motifs <章目录...> --gap-alert/--json`）；11 项新测试，全量 155 绿；设计沉淀见 docs/计算模型.md"跨章母题追踪"节。
2. **深层嵌套运行时验证**：v0.2 深层嵌套只存+展示（转换验证深度 1）。目标：可选的递归求真（`A believes B knows x` 的接地判定沿嵌套链展开），作为 inspect 的高置信模式，不改变默认确定性路径。**✅ 2026-08-29 完成**——`runtime/epistemics.py deep_grounding`：基础命题近似逐层接地（owner 隐式，knows/believes→集合成员，does_not_know→非成员；内层优先判定，holds/broken/unknown），`npl inspect --deep` 输出逐层 ✓/✗/？与总判定；深度≥3 中间层近似已在文档明示；10 项新测试，全量 165 绿；语义见 docs/计算模型.md"深层接地验证"节。
3. **快照续跑 / 分支（`npl continue --from 快照.json`）**：利用已有的 `from_dict` 反序列化，从任一状态快照起机继续执行新声明的场景；支持同一世界分岔出多条时间线后 diff 认知差异。这是"世界机"定位的自然延伸，也是远期互动叙事（AI DM）的地基。**✅ 2026-08-29 完成**——`executor.simulate_continue(state, scenes, start_index)`（编号从快照幕号+1 续接）+ `npl continue`（缺人物拒绝）/`npl branch-diff`（全量状态对比）+ 6 项端到端测试，全量 171 绿；演示 `examples/novel/chapter_extra.npl`（沉默线第 6 幕）branch-diff 分歧正确；语义见 docs/计算模型.md"快照续跑与分支"节。

### v0.4 — 世界动力学与题材包（✅ 2026-08-29 完成，193 测试全绿）

依据 2026-08-29 与用户的世界机设计讨论（详见 docs/计算模型.md"世界动力学"节）：

1. **世界动词族（ISA 扩展）✅**：`WORLD_SET_VERBS = {sets, clears}`——`主体.sets(事实)`/`主体.clears(事实)` 只改 `world.facts`，不触碰任何认知；两通道均生效；观察传播显式化（谁看见谁 CONFIRM/SUSPECT，无隐式转换）。设计收敛说明：原草案"自动按 access 传播观察后果"与"认知账目无隐式状态转换"哲学冲突，实现改为纯物理层动词+显式观察。
2. **世界事件主体 ✅**：sets/clears 主体可为已声明实体（`灵脉.sets(裂痕)`，无须入 participants）或人物；未声明主体 NAR-002、参数非已声明事实 NAR-002、嵌套参数拒绝。认知动词主体仍限人物。
3. **process 声明 ✅**：`process 名 { stage 名 { fact = 事实 } }`（阶段名唯一 NAR-012、标志事实必须已声明 NAR-002、空进程 NAR-041）；运行时事实仍由 sets/clears 显式驱动；`npl simulate`/`continue` 末尾按声明序单调性检查（后阶段真而前置未真 → 乱序告警）。语义说明：原草案"阶段自动触发、痕迹自动分发"改为声明式知识+显式驱动——保持世界机零隐式行为。
4. **题材包 ✅**：世界事实/实体/进程装进可 import 的 .npl（不装人物、场景、文风——避免跨文件 style/world 冲突），故事首行 `import "包.npl"` 获得整个世界的物理与历史。示例 `examples/packs/northern_veins/`（《塌》两幕，灵脉进程 + 世界动词 + 显式观察）。
5. **ISA 手册 ✅**：`docs/ISA手册.md`（ISA v0.4）——两条通道/五认知族+WORLD 族/嵌套参数/access/叙事指令/Reader Model 更新/确定性协议/静默行为表/演进规则（兼容性承诺：动词语义只增不改，旧程序新运行时逐字节一致）。
6. **目标驱动推演（候选，未做）**：`npl simulate --until "fact X = true"` 留待远期（与互动叙事前端一起做）。

### 已外移项目（不在本仓库做）

- **去 AI 味检测 / 成文质量系统**：2026-08-29 用户复盘后裁定——成文质量（文风、AI 腔、重写环）是世界认知之外的正交问题，移出本项目，另立独立成文系统；它以本引擎为下游消费方（CLI --json 契约）。调研成果（committee 数值指标/脚本门/打回重写环机制）保留在对话纪要，届时取用。

---

## 8. 评测方案（怎么证明"限制视角真的守住了"）

1. **规则层**：`npl check` 的 NAR 错误码命中（M2 起）。
2. **对照实验**（M2 核心实验）：同一火车站场景，A 组 = 裸 LLM + 全量设定 prompt；B 组 = NPL 管线。对两组输出统一跑命题抽取 + 认知检查，比较泄漏率。
3. **人工盲评**：随机打乱输出，评审"是否出现 POV 越权 / 角色全知 / 悬念提前泄底"，1-5 分制。
4. **确定性回归**：Runtime 层（simulate 路径）不依赖 LLM，快照必须可逐字节复现——这本身是"Runtime is the source of truth"的可测试表达。

---

## 9. 风险与对策

| 风险 | 对策 |
|---|---|
| 泄漏判定依赖 LLM 命题抽取，有误报/漏报 | 抽取与判定分离（抽取可换模型）；规则引擎兜底；定期人工抽检校准；抽取 prompt 固定版本化 |
| 语法设计过早僵化 | 版本号 + 迁移说明；派生原语"先语义后语法"；M0 不锁派生层 |
| LLM 无视 context 约束（知道了不该知道的还是写出来） | Context 物理隔离（deny 信息根本不进 prompt）+ 渲染后检查 + 带错误码重渲染循环（上限 3 次） |
| 中文指代/隐喻影响命题抽取 | 用 LLM 抽取而非关键词匹配；抽取时提供场景人物名单做 grounding |
| 成本与速度（每场景多次调用） | 渲染/抽取绑不同适配器（大模型渲染、本地模型抽取）；simulate 路径零 LLM 成本；缓存 |
| 范围蔓延（Reader Model、互动叙事、AI DM） | §1.3 非目标清单 + 里程碑门禁，新想法一律进 backlog 不进当前里程碑 |

---

## 10. 与既有资产的汇合点（远期）

- **叙事世界本体（Ontology）**：定义"存在什么"→ NPL 的 world 声明是其可执行化。
- **小说 Agent Committee**：Agent 不再是"讨论的创作者"，而是编译器的不同阶段（World Runtime / Mind Runtime / Narrative Runtime）。原 2M 字长篇的"文本灌水"问题被"小说 = Program，文本 = 编译产物"直接消解。
- **AI DM / 互动叙事**：同一 Narrative Runtime 换前端（玩家输入替代 Scene 程序），M3 的 Actor 模式即为其前身。**已确认为远期项目，不排期**；其地基（快照读档/分叉）在 v0.3 提前铺设。
- **文风指纹工具链**：v0.2 已实现可扩展指纹（特征注册表 + 词表注入）；独立的"成文质量/去 AI 味"系统外移（见 §7 已外移项目），以本引擎为下游。

---

## 附录 A：错误码体系（NAR-0xx）

编译类（M0 已实现，见 docs/语言规范.md §5）：

| 码 | 级别 | 含义 |
|---|---|---|
| NAR-001 | ERROR | 语法错误（非法 token / 非法产生式） |
| NAR-002 | ERROR | 引用未声明标识（pov / participants / information 字段 / access 主体 / 事件主体等） |
| NAR-003 | ERROR | 非法 access 引用（未知能力名，或 world 使用非 truth 能力） |
| NAR-004 | ERROR | 场景约束违例（pov / 事件主体 / emotional_arc 人物不在 participants） |
| NAR-010 | ERROR | 缺少版本声明（首行必须是 npl@0.1） |
| NAR-011 | ERROR | 顶层声明重名 |
| NAR-012 | ERROR | 块内重复字段 |
| NAR-013 | ERROR | 必填字段缺失（scene.pov / scene.participants / information.truth） |
| NAR-014 | ERROR | 嵌套认知引用错误（holder 未声明 / 最内层命题未声明）（M3） |
| NAR-042 | WARNING | 闪回场景含 REVEAL 类 information_changes（读者可能误置时序）（M3） |
| NAR-043 | WARNING | suspects 置信度非法（越界 / 非数值）（M3） |
| NAR-041 | WARNING | 自由命题：认知小节或 dramatic_goal 引用了未接地的 id |

运行时与检查器类（M1-M2 实现）：

| 码 | 类别 | 含义 |
|---|---|---|
| NAR-021 | 认知 | Epistemic leakage：POV 叙述泄露了权限外信息（对应设计对话中的示例） |
| NAR-022 | 认知 | 角色表现出了其认知状态中不存在的信息（角色全知） |
| NAR-031 | 叙事 | Conceal violation：违反 dramatic_goal 的 conceal 声明 |
| NAR-032 | 叙事 | Reveal 未达成：dramatic_goal 的 reveal 目标在场景中未被读者可得地呈现 |
| NAR-044（原 043 号让位于校验码） | 推断 | 证据不足推断：角色基于不足证据得出高置信结论（设计对话中的示例，附 confidence；待实现） |
| NAR-05x | 风格 | 风格约束违规（M4） |

## 附录 B：火车站场景（M1）验收清单

- [ ] B（Bao）的对话与行为不体现"知道 A 已经知情"
- [ ] A（Lin）不直接说出"我知道你把厂卖了"
- [ ] 叙述不进入 `deny = Bao.private_thought`（全文无 B 的内心独白）
- [ ] 无全知旁白（不出现"B 不知道的是，林早已看过合同"式句子）
- [ ] Lin 的情绪（grief→control→cold_clarity）仅通过可观察细节透出（restrained 风格）
- [ ] `reveal = meta_knowledge` 对读者成立：读者能感知到"她已经知道了"
- [ ] `conceal = lin_has_seen_contract_copy` 成立：读者不确定她是否握有实证
- [ ] `npl inspect` 输出正确（含 Bao 的虚假信念标注）✅ M1 已通过
- [ ] `npl simulate` 确定性回归通过（M2 后追加：`npl check` 0 ERROR）✅ M1 已通过（逐字节一致测试）
